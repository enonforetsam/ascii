{
  "name": "ascii",
  "version": "0.0.1",
  "description": "Everything is characters ~ a text-native generative art instrument. 12 engines, three modes, any charset, one HTML file.",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "deploy:staging": "npx wrangler deploy --env staging",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "puppeteer-core": "^25.2.1"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/enonforetsam/ascii"
  },
  "homepage": "https://ascii.krackeddevs.com"
}
